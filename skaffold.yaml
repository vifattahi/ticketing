apiVersion: skaffold/v4beta1
kind: Config
deploy:
  kubectl: {}
build:
  local:
    push: false
  artifacts:
    - image: vifattahi/auth
      context: auth
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
          - src: 'src/**/*.ts'
            dest: .
manifests:
  rawYaml:
    - ./infra/k8s/*
